/**
 * 全域常數定義
 * 集中管理應用中使用的魔術數字
 */

/** Box 尺寸 (px) */
export const BOX_SIZE = 40

/** 列編號區域寬度 (px) */
export const LABEL_WIDTH = 60

/** 刪除按鈕區域寬度 (px) */
export const DELETE_BTN_WIDTH = 80

/** 最大列數限制 */
export const MAX_ROWS = 1000

/** 最大 SubTab 數量 */
export const MAX_SUBTABS = 10

/** 預設 SubTab */
export const DEFAULT_SUBTAB = 1

/** localStorage key 前綴 */
export const STORAGE_KEY_PREFIX = 'box-table-'

/** 當前活動主模式的 localStorage key */
export const ACTIVE_MODE_KEY = 'box-table-active-tab'

/** 當前活動 SubTab 的 localStorage key 前綴 */
export const ACTIVE_SUBTAB_PREFIX = 'box-table-active-subtab-'
